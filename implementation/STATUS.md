# CommerceLens 实施记录

用户顺序：先完成后端，包括监控与恢复，再编写根 design.md 并实现前端。

## 已完成

- 所有专项文档、契约、资产说明和现有 Java/Nuxt 路径核查。
- 固定上游 SHA；原图控制器 6 项基线测试通过。

## 后端验收完成

- 数据导入、八指标查询、持久任务/审批/证据/报告、会话与权限、监控/恢复/API 已实现。
- 最终 74 项 Java 测试全部通过：查询11、导入8、运行44、API2、监控9；无失败、错误或跳过。新增22项覆盖复合指标识别、语义拒绝、SKU变化和全范围贡献分解；Checkstyle通过。
- MySQL8.4.11 三账号真实联调：完整参考 CSV 原始事实查询与 golden 一致；登录/CSRF、授权、审批冲突、独立图运行、证据、报告、导出、SSE重放、成员撤权、定时监控已通过。
- `scripts/verify-commerce.py` 在重启后的最新编译代码和已发布的 `demo_20260921_ui_v2` 上再次通过全部6组验证，记录见 `backend-http-validation.json`。
- MySQL只读账号执行无影响的 UPDATE ... WHERE 1=0 仍收到1142拒绝，证明数据库写权限被限制。
- 真实模型凭据未配置；模型适配器已实现，本次运行明确为 `DETERMINISTIC_LOCAL`，不计为真实模型评测。

## 前端验收完成

- 全部八张原型图已检查，根 design.md 已建立。
- 正式 Nuxt 七页面、登录、证据抽屉、规则与授权表单已经接入真实 API/SSE；开发开始于后端验收通过之后。
- 前端 53 项测试通过，其中 Commerce 金额与日期 28 项、SSE 8 项，既有测试 17 项。最终生产构建通过。
- 完整 Vue 类型检查在新增 Commerce 范围内零错误；全仓仍有 12 项上游既有类型错误，见 `acceptance.md`，未扩展修改旧功能。
- 浏览器已完成总览范围切换、审批运行、报告/导出反馈、24 条审计事件回放、指标查询、规则保存与真实告警知晓、成员列表和 390px 移动布局。
- 标准 ZIP 通过真实上传与发布，`demo_20260921_ui_v2` 八表共 389,208 行、质量 `COMPLETE`。水位和发布时间分别显示；旧快照与历史报告保持原版本。
- 详细记录见 `browser-validation.json` 和 `acceptance.md`。

## 固定集语义回归

- 首轮 48 项固定案例：21 通过、10 失败、17 明确跳过，记录在 `deterministic-evaluation-initial.json`，不计为真实模型评测。
- 最终真实 HTTP 复测为 **31 通过、0 失败、17 跳过**；原始问题及黄金断言未调整，首轮10项失败全部修复。结果见 `deterministic-evaluation.json`，运行快照为 `demo_20260921_ui_v2`。
- 跳过项需要专用边界/替代快照、可控故障、事件裁剪或浏览器状态；逐项原因随结果保存。临时成员已禁用，待审批任务已取消，清理无异常。这不是完整48项通过，也不计为真实模型评测。

本文件持续更新。模型凭据、真实平台数据和生产硬件未提供；不得将未执行的模型评测或性能目标描述为已通过。
