# Managed exclusively by the Biz Sentinel deployment.
server {
    listen 80;
    server_name bizsentinel.zimagent.top;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/biz-sentinel-acme;
        try_files $uri =404;
    }
    location / {
        return 404;
    }
}
